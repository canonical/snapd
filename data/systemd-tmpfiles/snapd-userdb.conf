# Ensure /run/systemd/userdb/ exists so that snapd.userdb-proxy.socket
# can bind its ListenStream unit within it. Some systems ship systemd
# but not systemd-userdbd, so the directory would otherwise not exist
# at boot.
d /run/systemd/userdb 0755 root root -
