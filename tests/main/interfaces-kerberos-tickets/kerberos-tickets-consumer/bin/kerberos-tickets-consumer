#!/bin/sh
ticket=${KRB5CCNAME#FILE:}
echo "KRB5CCNAME:$KRB5CCNAME"
cat "$ticket" 2>/dev/null || echo "Could not read ticket $ticket"
echo 8 2>/dev/null >>"$ticket" || echo "Could not write to ticket $ticket"
