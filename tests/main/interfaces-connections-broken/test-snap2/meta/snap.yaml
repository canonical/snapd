name: test-snap2
version: '1.0'
summary: Provider snap
description: Provider snap
base: core24
confinement: strict
grade: stable

slots:
    ciiface:
        interface: content
        content: broken-conn-test
        source:
          write:
            - $SNAP_DATA/data
apps:
  test:
    command: bin/test.sh