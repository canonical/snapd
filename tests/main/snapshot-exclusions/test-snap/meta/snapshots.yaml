exclude:
    # Remove all gif files
  - $SNAP_USER_COMMON/*.gif
  - $SNAP_USER_COMMON/*/*.gif
    # Just match a couple of individual files
  - $SNAP_USER_DATA/subdir/test_*.txt
  - $SNAP_USER_DATA/test_2.txt
    # We want to delete all the files in the subdirectories
  - $SNAP_COMMON/*/*
    # This should not affect subdirectories
  - $SNAP_DATA/*_1.txt
