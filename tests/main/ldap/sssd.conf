[sssd]
config_file_version = 2
services = nss, pam
domains = LDAP

[domain/LDAP]
id_provider = ldap
ldap_uri = ldap://${host_name}
cache_credentials = True
ldap_search_base = ${ldap_suffix}
auth_provider = krb5
krb5_server = ${host_name}
krb5_realm = ${realm_name}
