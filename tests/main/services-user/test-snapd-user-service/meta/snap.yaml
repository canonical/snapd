name: test-snapd-user-service
version: 1.0
apps:
    svc1:
        command: bin/start
        daemon: simple
        daemon-scope: user
    svc2:
        command: bin/start
        daemon: simple
        daemon-scope: user
    svc3:
        command: bin/start
        daemon: simple
    svc4:
        command: bin/start
        daemon: simple
