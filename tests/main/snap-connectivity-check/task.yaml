summary: Check that "snap connectivity-check" works

details: |
    Check the `snap debug connectivity` command works

execute: |
    echo "Ensure the snap connectivity-check command works"
    snap debug connectivity | MATCH " * PASS"
