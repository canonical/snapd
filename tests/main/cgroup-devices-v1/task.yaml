summary: measuring basic properties of device cgroup

# Disable the test on all systems that boot with cgroup v2
systems: [ -fedora-35-*, -fedora-36-*, -debian-11-*, -debian-sid-*, -arch-*, -opensuse-tumbleweed-*, -ubuntu-22.*, -ubuntu-23.*, -ubuntu-core-22-*, -centos-9-*]

execute: ./task.sh
