name: libs-provider2
version: 1.0
base: core24
slots:
  cuda-driver-libs:
    source:
      - $SNAP/libs
  egl-driver-libs:
    priority: 15
    client-driver: libmultiply.so
    source:
      - $SNAP/libs
