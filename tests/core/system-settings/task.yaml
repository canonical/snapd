summary: System settings work

prepare: |
    cp -aL /etc/hostname /tmp/hostname.bak

restore: |
    cp -a /tmp/hostname.bak /etc/hostname

execute: |
    echo "Check that setting the hostname works"
    echo coole-kiste > /etc/hostname

