summary: System settings work

prepare: |
    cp -a /etc/hostname /etc/hostname.bak

restore: |
    cp -a /etc/hostname.bak /etc/hostname

execute: |
    echo "Check that settings the hostname works"
    echo coole-kiste > /etc/hostname

