summary: Measure the time needed to get snap changes

execute: |
    . "$TESTSLIB/snaps.sh"
    . "$TESTSLIB/perf.sh"

    for i in $(seq "$PERF_ITERATIONS"); do
        measure_exec_time "snap changes" changes all
        measure_exec_time "snap changes core" changes core
    done
