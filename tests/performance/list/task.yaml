summary: Measure the time needed to list the installed snaps

execute: |
    . "$TESTSLIB/snaps.sh"
    . "$TESTSLIB/perf.sh"

    for i in $(seq "$PERF_ITERATIONS"); do
        measure_exec_time "snap list" list all
    done
