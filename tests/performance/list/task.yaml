summary: Measure the time needed to list the installed snaps

execute: |
    . "$TESTSLIB/snaps.sh"
    . "$TESTSLIB/perf.sh"

    for i in $(seq 30); do
        et=$(execution_time_ms "snap list")
        echo "$SPREAD_SYSTEM,list,all,$et,milliseconds" >> $RES_FILE
    done
