project: snapd

path: /root/spread

backends:
    openstack:
        key: '$(HOST: echo "$OS_CREDENTIALS_AMD64_PS6")'
        halt-timeout: 2h
        groups: [default]
        systems:
            - fedora-42-64:
                  image: snapd-spread/fedora-42-64

suites: 
    suite/:
        summary: fake suite
