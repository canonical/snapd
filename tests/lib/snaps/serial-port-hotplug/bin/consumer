#!/bin/sh
check_access() {
    DEVPATH=$1
    if test -w "$DEVPATH" ; then
        echo "Access to $DEVPATH ok"
    else
        echo "Access to $DEVPATH failed"
    fi
}

check_access /dev/ttyUSB0
check_access /dev/ttyUSB1
