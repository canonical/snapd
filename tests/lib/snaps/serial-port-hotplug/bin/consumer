#!/bin/sh
check_access() {
    DEVPATH=$1
    DATA=$2
    if echo "$DATA on $DEVPATH" > "$DEVPATH" ; then
        echo "Access to $DEVPATH ok"
    else
        echo "Access to $DEVPATH failed"
    fi
}

DATA="$1"
check_access /dev/ttyUSB0 "$DATA"
check_access /dev/ttyUSB1 "$DATA"
