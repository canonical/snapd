#!/usr/bin/env python3

import subprocess
import sys

def run(filename):
    subprocess.check_call("lpr -P PDF {}".format(filename), shell=True)

if __name__ == '__main__':
  sys.exit(run(sys.argv[1]))
