name: test-snapd-mixed-service
version: 2.0
apps:
    sys-service:
        command: bin/start
        daemon: simple
    user-service:
        command: bin/start
        daemon: simple
        daemon-scope: user
