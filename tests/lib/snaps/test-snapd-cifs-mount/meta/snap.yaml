name: test-snapd-cifs-mount
version: 1.0
summary: Basic cifs-mount snap
description: A basic snap which allows mounting and unmounting CIFS filesystems

apps:
    sh:
        command: bin/sh
        plugs: [cifs-mount, network-control]
