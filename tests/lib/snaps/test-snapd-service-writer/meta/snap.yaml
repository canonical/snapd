name: test-snapd-service-writer
description: |
  A snap crafted to verify various file accesses and transitions. This has a
  potential to find issues on systems where SELinux policy is used

version: 1.0
apps:
    writer:
        command: bin/start
        daemon: simple
