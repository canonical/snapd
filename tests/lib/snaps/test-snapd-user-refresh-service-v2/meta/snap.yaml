name: test-snapd-user-refresh-service
version: 2.0
apps:
    foo:
        command: bin/start
        daemon: simple
    boo-second:
        command: bin/start
        daemon: simple
    bar:
        command: bin/start
        daemon: simple
        daemon-scope: user
    baz-second:
        command: bin/start
        daemon: simple
        daemon-scope: user
