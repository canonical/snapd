name: test-snapd-mixed-service
version: 1.0
apps:
    sys-service:
        command: bin/start
        daemon: simple
    user-service:
        command: bin/start
        daemon: simple
        daemon-scope: user
