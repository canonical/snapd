#!/usr/bin/env python

import os
import sys

DEVICE='/dev/ttyZZZ999'

def run():
    tty = os.open(DEVICE, os.O_RDWR)
    value = os.read(tty, 100)
    os.close(tty)
    return value

if __name__ == '__main__':
    print(run())
