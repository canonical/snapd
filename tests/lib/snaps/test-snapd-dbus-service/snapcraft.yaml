name: test-snapd-dbus-service
version: 1.0
summary: Python based example dbus service
description: ...
plugs:
  test-snapd-dbus-service-plug:
    interface: dbus
    bus: session
    name: io.snapcraft.SnapDbusService
apps:
  test-snapd-dbus-service:
    command: bin/test-snapd-dbus-service
    plugs:
      - test-snapd-dbus-service-plug
      - network
      - network-bind
parts:
  test-snapd-dbus-service:
    plugin: python3
    stage-packages:
      - python3-dbus
      - python3-gi
      - gir1.2-glib-2.0
  copy:
    plugin: dump
    source: .
